<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :expression %><br />
    <%= f.text_field :expression %>
  </div>
  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_field :answer %>
  </div>
  <div class="field">
    <%= f.label :language_answer %><br />
    <%= f.text_field :language_answer %>
  </div>
  <div class="field">
    <%= f.label :correct_answer %><br />
    <%= f.check_box :correct_answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
